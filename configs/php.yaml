extends: bases/configure-make.yaml

name: php
description: PHP interpreter with a minimal CLI build

repository:
  url: https://github.com/php/php-src.git
  directory: php-src
  depth: 1

variables:
  bootstrap_command: ./buildconf --force
  configure_args: >-
    --disable-all
    --enable-cli
    --without-pear
